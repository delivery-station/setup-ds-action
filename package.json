{
  "name": "setup-ds-action",
  "version": "1.0.0",
  "description": "GitHub Action to setup Delivery Station CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist --source-map --license licenses.txt",
    "compile": "tsc",
    "package": "ncc build lib/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/delivery-station/setup-ds-action.git"
  },
  "keywords": [
    "actions",
    "delivery-station",
    "oci",
    "setup",
    "typescript"
  ],
  "author": "Delivery Station",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.9.3"
  }
}
